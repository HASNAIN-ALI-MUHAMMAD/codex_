--Features--
    --project type
        vanilla (a basic html,css,js project)
        nodejs(a nodejs project)



--Structure--
/src
    // contains the commands run by commander
/src2
    -- contains the file system logic
    -- creating and initializing project directory,accessing backend for additional funcs
    --- dirs
    /codecli
    -- run commands and log the command history
    
    /initializer
    -- the project initializer

    /project
    -- the main entry point for project initialization


